/* ============================================================
   Ícones — re-export do lucide-react com os nomes usados no app.
   Aliases mantêm a API antiga (PinIcon, BarsIcon...) e centralizam
   a escolha do ícone num lugar só. Todos herdam currentColor.
   ============================================================ */

export {
  MapPin as PinIcon,
  BarChart3 as BarsIcon,
  FileText as DocIcon,
  Bell as BellIcon,
  Settings as GearIcon,
} from "lucide-react";
